export function geoEncode(locations: string[]): void {
    console.log(locations);
    // adviceAsync(ids, query)
    //     .then(r => process.stdout.write(r.join("\n")))
    //     .catch((err: Error) => process.stderr.write(err.toString()));
}